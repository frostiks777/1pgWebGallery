# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this
repository. It used to be a blank template ("заполнить самому") — the actual content now
lives in the root `AGENTS.md` (project overview, tech stack, dev commands, structure,
deployment, and the `.agents/skills/` AI dev cycle), which the repo root `CLAUDE.md` already
imports. Import it from here too so both entry points stay in sync automatically:

@../AGENTS.md
